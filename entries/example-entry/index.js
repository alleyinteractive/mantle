/**
 * Example entry point to enqueue.
 *
 * Entries are automatically built with webpack when included inside the
 * ../entries directory. Enqueue them in app/providers/class-asset-service-provider.php
 */
